{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "salychain.serviceAccountName" . }}
  labels:
    {{- include "salychain.labels" . | nindent 4 }}
{{- end }}
