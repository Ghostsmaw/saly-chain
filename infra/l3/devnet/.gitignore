# Generated by scripts/deploy.sh (op-deployer) + scripts/up.sh.
# Contains chain genesis/rollup config, the engine-API JWT secret, and op-deployer
# state — never commit.
artifacts/

# Devnet secrets (L1 RPC + batcher/proposer/deployer keys).
.env

# Post-deploy manifest also ignored at repo root, repeated here for safety.
deployments.base-sepolia.json
