{
  "name": "@salychain/smoke-tests",
  "version": "0.1.0",
  "private": true,
  "description": "Staging/local smoke harness: health + partner login→intent→settle→webhook",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "test:live": "vitest run src/live",
    "clean": "rm -rf .turbo"
  },
  "devDependencies": {
    "@types/node": "^20.14.10",
    "typescript": "^5.6.2",
    "vitest": "^2.1.1"
  }
}
